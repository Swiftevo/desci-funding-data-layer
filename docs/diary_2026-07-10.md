# Project Diary - 2026-07-10

## Focus

Reviewed the project state after Gitcoin GG23 canonical merge work and prepared the repository documentation to reflect the new V1.5 data shape.

## Current State

The data layer now has:

```text
61 projects
70 participations
6 funding rounds
274 GG23 donation events
80 GG23 donor wallets
```

Spark S6 contributes:

```text
49 participations
```

Gitcoin GG23 contributes:

```text
21 participations
274 donation events
80 donor wallets
```

## Decisions Reaffirmed

GG23 canonical merge uses:

```text
9 confirmed matches reusing Spark DSPJ IDs
12 new project IDs from DSPJ-0050 to DSPJ-0061
```

LunCo remains:

```text
DSPJ-0050
```

but is related to:

```text
DSPJ-0028 LORS
```

The relationship is preserved in matching/allocation notes, not by merging IDs.

DeSci LATAM remains:

```text
DSPJ-0060
```

and is independent from DeSci Mexico.

## README / TODO Review

The local workspace did not have a top-level `README.md`, so a current-state README was added.

The README now reflects:

```text
V1.5 project-centric architecture
current project/participation counts
Spark S6 and Gitcoin GG23 coverage
donation/donor status
public preservation rules
latest snapshot
```

`docs/TODO.md` was updated to include:

```text
post-GG23 next tasks
donor table status
classification review queue
project_relationships consideration
next historical Gitcoin backfill planning
```

## Donor Database Status

The project has a GG23 donor draft table:

```text
data/donors/gitcoin_gg23_donors_draft.csv
data/donors/gitcoin_gg23_donors_draft.json
```

It contains:

```text
80 unique donor wallets
```

However, this is not yet a cross-round canonical donor database.

There is currently no:

```text
data/donors/donors_latest.csv
data/donors/donors_latest.json
```

Recommendation:

```text
Keep GG23 donor data as round-specific draft for now.
Promote to donors_latest after at least one more Gitcoin round is backfilled, unless donor analysis becomes the immediate priority.
```

## Next Suggested Step

After PR #4 is reviewed and merged:

```text
Generate classification_review_queue.csv
```

Then choose between:

```text
GG21 backfill
project_relationships table
donors_latest promotion
integrity dashboard / visual overview
```

